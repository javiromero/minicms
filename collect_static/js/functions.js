django.jQuery(document).ready( function() {
        
        // Enabling miniColors
        django.jQuery("#id_color_principal, #id_color_secundario").miniColors({
                letterCase: 'uppercase'
        }); 
        
});
